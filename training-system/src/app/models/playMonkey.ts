export class PlayMonkey {
  public column: number;
  public cell: number;
  public bananasCounter = 0;
  public harvestedBananas = 0;
  public maxIndex: number;
  public minIndex = 0;
  public rotation: string;

  // tslint:disable-next-line:max-line-length
  // constructor(column: number, cell: number, bananasCounter: number, harvestedBananas: number, maxIndex: number, minIndex: number, rotation: string) {
  //   this.column = column;
  //   this.cell = cell;
  //   this.bananasCounter = bananasCounter;
  //   this.harvestedBananas = harvestedBananas;
  //   this.maxIndex = maxIndex;
  //   this.minIndex = minIndex;
  //   this.rotation = rotation;
  // }
}
